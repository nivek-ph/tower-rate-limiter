# syntax=docker/dockerfile:1

FROM debian:bookworm-slim

RUN apt-get update \
    && apt-get install --yes --no-install-recommends bash ca-certificates curl wrk \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
COPY benchmarks ./benchmarks

CMD ["bash", "benchmarks/run.sh"]
