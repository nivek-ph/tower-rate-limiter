[package]
name = "tower-rate-limiter"
version = "0.1.4"
edition = "2024"
rust-version = "1.96.0"
description = "Keyed fixed-window rate limiting middleware for Tower"
readme = "README.md"
keywords = ["rate-limit", "tower", "axum", "redis"]
categories = ["web-programming::http-server"]
homepage = "https://github.com/nivek-ph/tower-rate-limiter"
repository = "https://github.com/nivek-ph/tower-rate-limiter"
license = "MIT OR Apache-2.0"
authors = ["nivek-ph <h499871809@gmail.com>"]
include = [
	"/src/**",
	"/examples/**",
	"/tests/**",
	"/Cargo.toml",
	"/README.md",
	"/LICENSE-APACHE",
	"/LICENSE-MIT",
]

[package.metadata.docs.rs]
all-features = true

[features]
default = ["memory"]
memory = ["dep:moka"]
redis = ["dep:redis"]
redis-lua = ["redis", "redis/script"]
runtime-tokio = ["redis?/tokio-comp"]
runtime-smol = ["redis?/smol-comp"]
axum = ["dep:axum", "http-extract/axum"]
tracing = ["dep:tracing"]

[dependencies]
http = "1"
http-extract = { version = "0.1.2", default-features = false, features = [
	"client-ip",
] }
pin-project-lite = "0.2"
thiserror = "2"
tower = { version = "0.5", features = ["util"] }
tracing = { version = "0.1", default-features = false, features = [
	"std",
], optional = true }

moka = { version = "0.12", features = ["sync"], optional = true }

# optional dependencies
axum = { version = "0.8", optional = true }
redis = { version = "1", default-features = false, optional = true }

[dev-dependencies]
dotenvy = "0.15"
smol = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[[example]]
name = "tower_memory"
required-features = ["memory"]

[[example]]
name = "tower_dynamic"
required-features = ["memory"]

[[example]]
name = "axum_memory"
required-features = ["axum", "memory"]

[[example]]
name = "trusted_proxy_client_ip"
required-features = ["axum", "memory"]

[[example]]
name = "axum_redis"
required-features = ["axum", "redis", "runtime-tokio"]

[[test]]
name = "axum"
required-features = ["axum"]

[[test]]
name = "redis_store_tokio"
path = "tests/redis/tokio.rs"
required-features = ["redis", "runtime-tokio"]

[[test]]
name = "redis_store_smol"
path = "tests/redis/smol.rs"
required-features = ["redis", "runtime-smol"]
